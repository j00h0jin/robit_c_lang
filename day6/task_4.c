/*
회문 판별하기
• 회문 또는 팰린드롬(Palindraome)은 앞 뒤 방향으로 볼 때 같은 순서의 문자로 구성된 문자열을말한다. • Ex) abba, madam,
was it a cat i saw • 문자열을 입력받아 회문인지 아닌지 판별하여 출력 • hint : • 스택과 큐를 이용(LIFO, FIFO)
*/

#include <stdio.h>

void main()
{
    printf("test");
}