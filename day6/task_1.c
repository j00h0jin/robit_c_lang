/*
단순 연결 리스트 구현
• insert : 원하는 위치에 node 추가(그 전 data와 index모두 가능하도록) • insert_back : 연결리스트의 맨 끝에 node 추가
• insert_first : 연결리스트의 맨 처음에 node 추가
• delete : 원하는 요소 삭제(원하는 요소는 data와 index모두 가능하도록) • delete_first : 연결리스트 맨 처음 node 삭제
• delete_back : 연결리스트 맨 마지막 node 삭제
• get_entry : 요소 찾기(data로 찾을 시 index 반환, index로 찾을 시 data 반환) • get_length : 리스트 전체 길이 반환
• print_list : 리스트의 모든 요소 출력
• reverse : 리스트 역순으로 만들기
• Data 자료형은 자유
*/

#include <stdio.h>

void main()
{
    printf("test");
}