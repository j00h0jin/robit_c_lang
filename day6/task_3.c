/*
단순 연결 리스트를 이용한 Queue 구현
• Enqueue : Queue에 data 입력
• Dequeue : Dequeue하고 Dequeue된 값 출력. Queue가 비어있을 시 비어있다고 출력
• size : Queue 크기 출력
• front : front에 위치한 값 반환. • rear : rear에 위치한 값 반환. • isEmpty : Queue에 데이터가 없으면 true, 있으면 false
반환 • printQueue : Queue 내 모든 값 출력. Queue가 비어있을 시 비어있다고 출력
*/

#include <stdio.h>

void main()
{
    printf("test");
}